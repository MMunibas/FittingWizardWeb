subprojects {
    group 'ch.unibas.fitting'
    version '2.0.0'

    apply plugin: 'java'

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }

    sourceCompatibility = 1.10

    repositories {
        mavenCentral()
    }
}

