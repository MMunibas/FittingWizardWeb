subprojects {
    group 'ch.unibas.fitting'
    version '0.6-BETA'

    apply plugin: 'java'

    compileJava.options.encoding = 'UTF-8'

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

